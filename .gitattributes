# Windows 배치 파일은 CRLF 로 고정한다. cmd.exe 는 LF 만 있는 .bat 의
# 레이블과 goto 를 환경에 따라 다르게 처리한다 - 체크아웃 환경에 맡기지 않는다.
*.bat text eol=crlf

# 셸 스크립트는 반대로 LF 로 고정한다. macOS 런처가 추가되면 여기에 걸린다.
*.sh text eol=lf
*.command text eol=lf
